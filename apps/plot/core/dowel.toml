[package]
name    = "plot-core"
version = "0.2.0"
edition = "2026"

[features]
default  = []
# 検査用。実行時の誤りを黙らせずに落とす。配る版には入れない。
sanitize = []

# 描画は cairo に任せる。窓のことは知らない——それが分離の要点である。
[[dependencies]]
name    = "cairo"
version = "1.16"
