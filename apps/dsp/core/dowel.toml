[package]
name    = "dsp-core"
version = "0.1.0"
edition = "2026"
# 三つ組ごとの道具立ては、この木の3つのパッケージで1つの表を共有する
# （ADR-0033）。以前は使う側それぞれが同じ表を書き写していた。
toolchains = "../toolchains.toml"

# `targets` は宣言しない。手元の機械（x86_64）へも組むためである。
# 宣言すると、そこに書いた三つ組しか受け付けなくなる。
