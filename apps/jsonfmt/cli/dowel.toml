[package]
name    = "jsonfmt"
version = "0.2.0"
edition = "2026"

[features]
default = []
# 深い入力を扱う版。core の同名の機能へそのまま渡す。
deep    = ["jsonfmt-core/deep"]

[[dependencies]]
name = "jsonfmt-core"
path = "../core"
