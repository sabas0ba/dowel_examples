[package]
name    = "gated"
version = "0.0.0"

[features]
default = []
quiet   = []

# 無効な optional 依存は辺もノードも作らない。読み込みすら行われないため、
# pkg-config も引かれないはずである（ADR-0015）。
[[dependencies]]
name     = "quiet"
version  = "1.0"
optional = true
