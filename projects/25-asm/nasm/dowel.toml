[package]
name    = "nasmx"
version = "0.1.0"
edition = "2026"

# 宣言された assembler。空なら C の駆動器が組み立てる（ADR-0048 のまま）
# という既定であり、`link` と同じ形をしている。既定を `nasm` にすると
# ほとんどの木で誤りになるので、この道具は**宣言されるために在る**。
[toolchain.x86_64-unknown-linux-gnu]
c   = "cc"
asm = "nasm"
