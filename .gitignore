.work/
.dowel-src/
__pycache__/

# dowel が解決したシステム依存を記録するもの。手元で組むと生成される。
# 記録される版はその機械の pkg-config が答えたものなので、commit すると
# 別の機械で走らせたときに「環境が変わった」として毎回引っかかる。
# 意図して固定したい fixture があれば `git add -f` で明示的に加える。
dowel.lock
